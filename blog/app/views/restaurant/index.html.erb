<h1> lunch Index </h1>
<ul>
  <li><%= link_to "Lunch" %></li>
</ul>

<table>
  <thead>
  <th>name</th>
  <th>location</th>
  <th>recommend_menu</th>
  <th>recommend_cnt</th>
  <th>genre</th>
  </thead>
  <tbody>

  <% @restaurant.each do |restaurant| %>
    <tr>
      <td><%= link_to restaurant.name, "/restaurant/#{restaurant.id}/edit" %></td>
      <td><%= restaurant.location %></td>
      <td><%= restaurant.recommend_menu %></td>
      <td><%= restaurant.recommend_cnt %></td>
      <td><%= restaurant.genre %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= button_to "Create Restaurant", new_restaurant_path, :method => "get" %>

<%= form_with url: "/restaurant/Test" do |form| %>
  <div>
    <%= form.submit "Post /restaurant/Test" %>
  </div>
<% end %>

<script>
</script>
<style>
    h1 {
        font-size : 35px;
        text-align : center;
    }

    table {
        font-family: Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    table td, #customers th {
        border: 1px solid #ddd;
        padding: 8px;
    }

    table tr:nth-child(even){background-color: #f2f2f2;}

    table tr:hover {background-color: #ddd;}

    table th {
        padding-top: 12px;
        padding-bottom: 12px;
        text-align: center;
        background-color: #04AA6D;
        color: white;
    }

    button {
        background-color: #4CAF50; /* Green */
        border: none;
        color: white;
        padding: 15px 32px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
    }
</style>